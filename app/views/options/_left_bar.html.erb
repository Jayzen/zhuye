<div class="card">
  <div class="card-body d-flex flex-column">
    <div class="mt-2">
      <%= link_to "模块显示设置", options_path, class: "btn #{ (action_name == 'index') ? 'btn-primary' : 'btn-outline-secondary' }  btn-block" %>
    </div>
  </div>
</div>
