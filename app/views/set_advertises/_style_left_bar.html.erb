<div class="card">
  <div class="card-body d-flex flex-column">
    <div class="mt-2">
      <a href="<%= contact_set_advertises_path %>">
        <button type="button" class="btn <%= (action_name == 'contact') ? 'btn-primary' : 'btn-outline-secondary' %> btn-block">基本信息栏设置</button>
      </a>
    </div>
    <div class="mt-2">
      <a href="<%= map_height_set_advertises_path %>">
        <button type="button" class="btn <%= (action_name == 'map_height') ? 'btn-primary' : 'btn-outline-secondary' %> btn-block">地图高度设置</button>
      </a>
    </div>
  </div>
</div>
